
# 2.6 当前阶段建议你继续冻结的下一批关键问题

在这 6 项之后，接收端还必须继续明确下面这些问题，否则还不能真正开工。

## 第一组：重组与提交边界

* 接收端最终输出单位是 PRT 还是 CPI     // 当前选择为CPI

  ---

    Packet → Channel Fragment → PRT → CPI

    其中：

    Packet：UDP 分包
    Channel Fragment：某个 PRT 下某通道的分片集合
    PRT：某个 CPI 内的一次脉冲重复周期原始数据
    CPI：最终交付给信号处理消费者的单位

    所以：
    内部重组最小闭环：PRT
    外部输出最终单位：CPI

  ---

* 缺一个包时，是丢整个 PRT，还是带缺陷提交      // 请求讨论最佳实践。

```txt

```

* 尾包缺失时是否允许逻辑收尾    // 解释并且给出最佳实践。








## 第二组：缓存与超时策略

* 一个 `(CPI, PRT)` 的重组缓存保留多久
* 新 CPI 到来时，旧 CPI 未完成数据如何处理
* 波位切换时，旧波位残留缓存如何处理

## 第三组：异常处理策略

* 非法包直接丢弃还是记录后旁路
* 包序异常是否触发整 PRT 失效
* 未来包/过期包如何分类

## 第四组：输出接口定义

* 输出结构体长什么样
* 数据存储方式是拷贝还是零拷贝引用
* 完整性标记怎么编码

---
