# Project_Skeleton.md — QDGZ300 全系统 C++ 项目骨架文档

> **版本** v1.0 | **日期** 2026-03-04
> **技术栈** C++17 / ARM64 (aarch64) / Clang 18 / CoreX SDK 4.3.8 / CMake ≥ 3.16 / 银河麒麟 Linux Kernel 4.19
> **权威来源** v3.1_初次定版.md (协议仲裁) → M01_Receiver_Design.md 等分块文档 → 顶层设计系列 → 推断

> 重要说明：本文是“项目骨架/规划视图”，不是代码现状清单。当前仓库中只有部分模块真实落地；凡涉及是否已实现、接口是否存在、文件是否真实参与构建，均以 `CMakeLists.txt` 与对应源码目录为准。

---

## 目录

| 章 | 标题 |
|---|---|
| 1 | 顶层目录树 |

---

## 1. 顶层目录树

```
qdgz300_backend/
├── CMakeLists.txt                        # 顶层 CMake（定义全部 target 与交叉编译参数）
├── cmake/
│   ├── aarch64-toolchain.cmake           # ARM64 交叉编译工具链
│   ├── FindIluvatarCorex.cmake           # CoreX SDK / CUDA 查找脚本
│   └── CompilerOptions.cmake             # 统一编译选项（-Wall -Wextra -O2 -march=armv8-a）
├── proto/
│   └── hmi_protocol.proto                # Protobuf 定义（TrackFrame / Command / StateSnapshot）
├── include/
│   ├── qdgz300/common/                   # ═══ 全局公共头 ═══
│   │   ├── types.h                       # RawBlock, PlotBatch, Track, TrackFrame 等冻结结构
│   │   ├── spsc_queue.h                  # SPSC Lock-free Ring Buffer 模板
│   │   ├── memory_pool.h                 # NUMA-aware 内存池模板
│   │   ├── numa_utils.h                  # NUMA 绑定辅助工具
│   │   ├── metrics.h                     # atomic 指标基类（AtomicCounter / AtomicGauge）
│   │   ├── event_types.h                 # 事件枚举 + AlertEvent JSON 结构
│   │   ├── system_state.h                # 5-State FSM 枚举 + 19 条转移规则
│   │   ├── config_snapshot.h             # 配置快照结构（版本化 + 原子交换）
│   │   ├── time_types.h                  # 时间质量 Q0/Q1/Q2 + clock_domain
│   │   ├── error_codes.h                 # 统一错误码
│   │   └── constants.h                   # 冻结常量（T_reasm, MAX_TOTALFRAGS 等）
│   │
│   ├── qdgz300/m01_receiver/             # ═══ M01 接收模块 ═══
│   │   ├── udp_receiver.h                # UDP 批量收包（recvmmsg）
│   │   ├── packet_parser.h               # 32B Common Header 解析
│   │   ├── validator.h                   # Magic/Version/PacketType/PayloadLen 校验
│   │   ├── dispatcher.h                  # 2-way PacketType 分发（Data/Heartbeat）
│   │   ├── reassembler.h                 # 应用层分片重组引擎
│   │   ├── heartbeat_handler.h           # 心跳就地解析 + CRC32C 校验
│   │   ├── snapshot_extractor.h          # 尾包 40B Execution Snapshot 提取
│   │   ├── receiver_thread.h             # 单阵面接收主线程封装
│   │   └── receiver_metrics.h            # D-M-G1~G11 / D-M-H1~H5 指标集
│   │
│   ├── qdgz300/m02_signal_proc/          # ═══ M02 GPU 信号处理 ═══
│   │   ├── gpu_dispatcher.h              # GPU 调度主循环（Round-Robin 3 Streams）
│   │   ├── cuda_stream_pool.h            # CUDA Stream + Event 资源池
│   │   ├── gpu_pipeline.h                # H2D → Compute → D2H 三段流水
│   │   ├── signal_kernels.h              # FFT/CFAR/MTD 内核声明（stub）
│   │   ├── pinned_buffer_pool.h          # Node1 CUDA Pinned Memory 双缓冲池
│   │   └── signal_proc_metrics.h         # GPU 利用率、超时、队列深度指标
│   │
│   ├── qdgz300/m03_data_proc/            # ═══ M03 航迹处理 ═══
│   │   ├── tracker.h                     # 单阵面跟踪器（预测→关联→更新→维护）
│   │   ├── kalman_filter.h               # Kalman 滤波（predict/update）
│   │   ├── plot_associator.h             # 最近邻 / GNN 点航关联
│   │   ├── fusion_engine.h               # 跨阵面融合（ID 继承 / 合并 / 接力）
│   │   ├── track_manager.h               # Track 生命周期 FSM（TENT→CONF→COAST→LOST→DEL）
│   │   ├── track_frame_builder.h         # 全量快照生成 + 质量标志 + 覆盖位图
│   │   └── data_proc_metrics.h           # 关联成功率、航迹数、融合次数指标
│   │
│   ├── qdgz300/m04_gateway/              # ═══ M04 网关 ═══
│   │   ├── tcp_server.h                  # TCP 控制面（epoll 驱动）
│   │   ├── udp_sender.h                  # UDP TrackData 推送
│   │   ├── hmi_session.h                 # 连接管理 + Hello→Welcome→StateSnapshot 握手
│   │   ├── trackdata_serializer.h        # Protobuf 序列化 + ≤1200B 分片
│   │   ├── output_throttle.h             # 4 态流控（NORMAL/CONGESTED/BLOCKED/OFFLINE）
│   │   └── gateway_metrics.h             # 发送帧率、截断次数、连接状态指标
│   │
│   ├── qdgz300/orchestrator/             # ═══ 编排器 ═══
│   │   ├── orchestrator.h                # 主编排循环（MPSC 事件消费 + 定时器）
│   │   ├── state_machine.h               # 5-State FSM + 19 条 Decision Matrix
│   │   ├── event_dispatcher.h            # 事件路由 + 节流（去重窗口 + 流量限制）
│   │   ├── command_bridge.h              # HMI↔DACS 幂等桥接 + ACK 聚合
│   │   └── orchestrator_metrics.h        # 状态转移次数、事件处理延迟指标
│   │
│   ├── qdgz300/config/                   # ═══ 配置管理 ═══
│   │   ├── config_manager.h              # 版本化快照 + RCU-style 读 + 分层变更路由
│   │   ├── config_validator.h            # 范围/类型/冻结参数保护
│   │   └── param_meta_table.h            # 参数元表结构定义
│   │
│   ├── qdgz300/time_sync/                # ═══ 时间同步 ═══
│   │   ├── time_sync_manager.h           # 4 态 FSM（UNSYNC/SYNCED/HOLDOVER/EXPIRED）
│   │   └── time_quality_mapper.h         # Q0/Q1/Q2 → clock_domain 映射
│   │
│   ├── qdgz300/health/                   # ═══ 健康检查 ═══
│   │   ├── health_check.h               # 统一健康检查入口
│   │   ├── boot_checker.h               # Boot 6 项检查
│   │   └── runtime_monitor.h            # 100ms 周期轮询（gap / timeout / 队列 / 内存）
│   │
│   ├── qdgz300/diagnostics/              # ═══ 诊断中心 ═══
│   │   ├── diagnostics_center.h          # 事件 Ring Buffer + JSON 诊断包生成
│   │   └── diagnostics_rules.h           # 诊断规则定义
│   │
│   ├── qdgz300/recorder/                 # ═══ 录制器 ═══
│   │   ├── recorder.h                    # 消费 Rec_Q ×3 + 异步刷盘
│   │   └── disk_writer.h                # 磁盘写入 + 配额循环覆写
│   │
│   └── qdgz300/logging/                  # ═══ 日志与监控导出 ═══
│       ├── async_logger.h                # spdlog 封装，JSON 格式，per-thread buffer
│       └── metrics_exporter.h            # Prometheus text format 导出
│
├── src/                                  # ═══ 对应 include 的实现 ═══
│   ├── common/
│   │   ├── spsc_queue.cpp                # 仅显式实例化（模板主体在头文件）
│   │   ├── memory_pool.cpp
│   │   ├── numa_utils.cpp
│   │   └── metrics.cpp
│   ├── m01_receiver/
│   │   ├── udp_receiver.cpp
│   │   ├── packet_parser.cpp
│   │   ├── validator.cpp
│   │   ├── dispatcher.cpp
│   │   ├── reassembler.cpp
│   │   ├── heartbeat_handler.cpp
│   │   ├── snapshot_extractor.cpp
│   │   ├── receiver_thread.cpp
│   │   └── receiver_metrics.cpp
│   ├── m02_signal_proc/
│   │   ├── gpu_dispatcher.cpp
│   │   ├── cuda_stream_pool.cpp
│   │   ├── gpu_pipeline.cpp
│   │   ├── signal_kernels.cu            # CUDA 内核实现（CoreX SDK 编译）
│   │   ├── pinned_buffer_pool.cpp
│   │   └── signal_proc_metrics.cpp
│   ├── m03_data_proc/
│   │   ├── tracker.cpp
│   │   ├── kalman_filter.cpp
│   │   ├── plot_associator.cpp
│   │   ├── fusion_engine.cpp
│   │   ├── track_manager.cpp
│   │   ├── track_frame_builder.cpp
│   │   └── data_proc_metrics.cpp
│   ├── m04_gateway/
│   │   ├── tcp_server.cpp
│   │   ├── udp_sender.cpp
│   │   ├── hmi_session.cpp
│   │   ├── trackdata_serializer.cpp
│   │   ├── output_throttle.cpp
│   │   └── gateway_metrics.cpp
│   ├── orchestrator/
│   │   ├── orchestrator.cpp
│   │   ├── state_machine.cpp
│   │   ├── event_dispatcher.cpp
│   │   ├── command_bridge.cpp
│   │   └── orchestrator_metrics.cpp
│   ├── config/
│   │   ├── config_manager.cpp
│   │   ├── config_validator.cpp
│   │   └── param_meta_table.cpp
│   ├── time_sync/
│   │   ├── time_sync_manager.cpp
│   │   └── time_quality_mapper.cpp
│   ├── health/
│   │   ├── health_check.cpp
│   │   ├── boot_checker.cpp
│   │   └── runtime_monitor.cpp
│   ├── diagnostics/
│   │   ├── diagnostics_center.cpp
│   │   └── diagnostics_rules.cpp
│   ├── recorder/
│   │   ├── recorder.cpp
│   │   └── disk_writer.cpp
│   ├── logging/
│   │   ├── async_logger.cpp
│   │   └── metrics_exporter.cpp
│   └── main.cpp                          # 主入口，初始化所有模块
│
├── tests/
│   ├── unit/                             # 按模块子目录
│   │   ├── common/
│   │   ├── m01_receiver/
│   │   ├── m02_signal_proc/
│   │   ├── m03_data_proc/
│   │   ├── m04_gateway/
│   │   ├── orchestrator/
│   │   ├── config/
│   │   ├── time_sync/
│   │   ├── health/
│   │   └── recorder/
│   ├── integration/                      # 跨模块集成测试
│   │   ├── boot_to_standby_test.cpp
│   │   ├── warmup_to_running_test.cpp
│   │   ├── degraded_recovery_test.cpp
│   │   └── end_to_end_test.cpp
│   └── protocol/                         # PROTO 系列一致性测试
│       ├── proto_001_reorder_missing.cpp
│       ├── proto_002_reasm_timeout.cpp
│       ├── proto_003_key_mismatch.cpp
│       └── proto_004_late_fragment.cpp
│
├── benches/                              # 性能基准
│   ├── spsc_queue_bench.cpp
│   ├── reassembler_bench.cpp
│   ├── gpu_pipeline_bench.cpp
│   └── end_to_end_latency_bench.cpp
│
├── tools/
│   ├── fpga_emulator/                    # FPGA 模拟器（测试注入）
│   │   ├── emulator_main.cpp
│   │   └── packet_generator.cpp
│   └── towerguard/                       # 守护进程
│       ├── towerguard_main.cpp
│       ├── process_monitor.cpp
│       └── auto_restart.cpp
│
└── config/
    ├── default.yaml                      # 默认运行配置
    └── param_schema.yaml                 # 参数元表 schema
```

---
