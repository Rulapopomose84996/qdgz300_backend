# QDGZ300 Backend 重构工作入口

> 用途：进入 `refactor/kylin-v10-arm64-cuda-clang18` 分支后，先看这份文档恢复上下文。
> 维护规则：每轮工作结束后，更新“当前状态”“下一步”“关键命令”“风险与备注”。

---

## 1. 当前状态

- 当前工作分支：`refactor/kylin-v10-arm64-cuda-clang18`
- 当前主线任务：按照《同类项目构建与组织架构重构指南（麒麟V10_ARM64）》对仓库做分阶段重构
- 本项目特例约束：
  - 信号处理模块涉及 CUDA 兼容链路
  - 目标服务器按 2026 年 1 月 16 日基线文档要求，使用 `clang 18.1.8 + CoreX 4.3.8 + Iluvatar MR100/MR-V100 兼容栈`
  - 仍需兼容 `CMake 3.16.5`、`aarch64`、麒麟 V10 原生构建
- 当前阶段：Phase 3，模块级 `CMakeLists.txt`、`tests/unit` / `tests/integration` 和正式构建脚本已落地，下一步进入脚本验证与部署闭环

---

## 2. 已确认的重构硬约束

- 顶层 `CMakeLists.txt` 只保留编排职责，不继续堆业务源码清单
- 依赖优先走项目内离线化，避免依赖服务器在线安装开发包
- 构建链分三层：
  - 本地开发构建
  - WSL/Linux 交叉构建
  - 麒麟 V10 ARM64 服务器原生构建
- 正式测试入口固定到测试子目录，不依赖顶层 `ctest` 递归发现
- 运行时数据必须与源码目录隔离，统一收敛到 `data/`
- GPU/CUDA 相关模块必须显式兼容 CoreX 编译链，不默认沿用 GCC 路线

---

## 3. 建议的重构顺序

1. 先固化工作入口、部署基线、项目规划文档
2. 再拆顶层 CMake，为模块化装配做准备
3. 然后重组 `src/` 与 `tests/` 目录
4. 补齐服务器原生构建和交叉构建脚本
5. 最后整理 `deploy/`、配置、systemd、环境变量和运行时目录

---

## 4. 当前建议的下一步

- 第一优先级：验证 `scripts/build_production.sh` 和 `scripts/dev_build_wsl_cross.sh` 在真实环境可用
- 第二优先级：补齐部署安装脚本和服务目录布局
- 第三优先级：继续清理空壳模块和重复目录

---

## 5. 关键文档入口

- [同类项目构建与组织架构重构指南（麒麟V10_ARM64）](../../同类项目构建与组织架构重构指南（麒麟V10_ARM64）.md)
- [QDGZ300 麒麟V10 ARM64 CoreX/CUDA 部署基线](../部署基线/麒麟V10_ARM64_CoreX_CUDA_部署基线.md)
- [QDGZ300 重构分阶段计划](../项目规划/QDGZ300_重构分阶段计划.md)
- [服务器环境基线文档 (V2.0)](../冻结的设计资产/202601160034_Baseline_TowerGuard_Infrastructure_服务器环境基线%20(V2.0).md)

---

## 6. 关键命令

### 6.1 查看当前分支与工作区

```bash
git status --short --branch
```

### 6.2 后续正式服务器构建入口（待脚本固化）

```bash
bash scripts/build_production.sh
```

### 6.3 后续正式测试入口（待目录重构完成后固定）

```bash
ctest --test-dir build_production/tests/unit --output-on-failure
```

---

## 7. 风险与备注

- 当前顶层 CMake 仍是“大一统”结构，后续拆分时要避免一次性搬迁太多源文件
- 当前 `tests/` 仍未按 `unit/integration` 分层，正式测试入口尚未固化
- M02 信号处理模块涉及 `.cu` 与 CoreX 兼容链，需要单独验证 `clang++` 编译参数与链接路径
- 参考指南默认使用 GCC 7.3.0，但本项目在 GPU 链路上不能照搬该约束
